## Docker Notes

The `docker/` folder is retained for local container-based development only.

Files:

- `docker-compose.yml`
- `start-backend.sh`
- `wait-for-it.sh`

This repo’s primary production target is now Render via [render.yaml](/home/lus/Downloads/yuop/render.yaml), not the legacy Docker deployment path.

